# 答案 001：示例状态映射

## 期望结论

当前知识库不能确认 `Draft` 就是“待处理”，因此也不能据此确认 `Draft` 对象是否可以提交。

## 必须引用

- `WIKI/概念/示例概念.md`：专家口径只确认“待处理只能查看、不能提交”，但没有说明它对应 `Pending` 还是 `Draft`。
- `WIKI/功能模块/示例系统/示例菜单/示例页面.md`：截图显示 `Draft`、`Pending`、`Completed` 样本，但没有展示状态下拉或提交结果。
- `WIKI/待确认项/待确认项_index.md`：`DEMO-001` 正在等待业务确认。

## 必须说明的边界

- `待业务确认` 不是已确认规则。
- 不能只按英文词义把 `Draft` 或 `Pending` 直接映射为“待处理”。
- 在业务口径确认和页面实采完成前，应回答“当前无法确认”，并建议补齐状态映射和提交入口证据。

## 不应默认读取

- `.work/`
- `RAW_SOURCES/`
- `WIKI/log.md`
- `文档备份/`
