# 截图示例目录

这里存放系统页面、弹窗、侧拉框、列表、配置项等原始截图。

示例路径：

```text
RAW_SOURCES/截图/示例系统/示例菜单/示例页面/示例页面截图.png
```

截图要求：

- 使用虚构数据或已脱敏数据。
- 遮挡人员姓名、手机号、邮箱、账号、客户名称、单号、金额等敏感信息。
- 使用 `YYYY-MM-DD_HHMMSS_<状态>_<序号>` 等唯一名称；已有文件绝不覆盖。
- 候选截图先放在 `.work/page-capture/`；这里只保留能支撑页面结构、关键字段、动态联动、风险提示或业务差异的最小集合。
